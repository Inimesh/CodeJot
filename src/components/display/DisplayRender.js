import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const DisplayRender = ({ text }) => {
  return (
    <View>
      <Text>{text}</Text>
    </View>
  )
}

export default DisplayRender

const styles = StyleSheet.create({})